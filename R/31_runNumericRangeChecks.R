#' runNumericRangeChecks Run a check to ensure numeric variables fall within an appropriate range
#'
#' @return Results of the check
runNumericRangeChecks <- function(){
  
  # .checkOutput <- list(
  #   "checkTitle" = .checkTitle
  #   ,"checkDescription" = .checkDescription
  #   ,"checkShortDescription" = .checkShortDescription
  #   ,"pass" = ifelse(nrow(.checkDs) > 0, FALSE, TRUE)
  #   ,"values" = list(
  #     "n" = nrow(.checkDs)
  #     ,"N" = nrow(dsToCheck)
  #     ,"pct" = nrow(.checkDs) / nrow(dsToCheck)
  #   )
  #   ,"listing" = .checkDs
  # )
  
  return(NULL)
}
